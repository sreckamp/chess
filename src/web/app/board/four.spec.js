'use strict';

describe('chess.four module', function() {

  beforeEach(module('chess.four'));

  describe('four controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var controller = $controller('FourPlayerController');
      expect(controller).toBeDefined();
    }));

  });
});